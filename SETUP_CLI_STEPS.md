# Пошаговая настройка Supabase CLI

## Шаг 1: Логин в Supabase

Откройте ваш терминал (Git Bash, PowerShell или CMD) и выполните:

```bash
npx supabase login
```

Это откроет браузер для авторизации. После успешной авторизации вы увидите сообщение:
```
> Finished supabase login. You are now logged in.
```

## Шаг 2: Найти project-ref

1. Откройте Supabase Dashboard: https://app.supabase.com
2. Выберите ваш проект "EduMap"
3. В URL вы увидите: `https://app.supabase.com/project/YOUR_PROJECT_REF`
   - ИЛИ
4. Перейдите в Settings → General → Reference ID

Скопируйте `YOUR_PROJECT_REF` (это строка типа: `abcdefghijklmnop`)

## Шаг 3: Связать проект

В терминале выполните (замените `YOUR_PROJECT_REF` на ваш реальный ID):

```bash
npx supabase link --project-ref YOUR_PROJECT_REF
```

Вас попросят ввести Database password (тот, который вы указали при создании проекта).

После успешной привязки вы увидите:
```
> Finished supabase link.
```

## Шаг 4: Проверить настройку

```bash
npx supabase projects list
```

Должен показать ваш проект.

## Шаг 5: Выполнить миграции

Теперь можно автоматически выполнить миграции:

```bash
# Пересоздать базу (если нужно начать заново)
npm run db:reset-cli

# Или просто применить миграции
npm run db:push
```

## Что произойдет

Когда вы выполните `npm run db:push`, CLI:
1. Автоматически найдет все файлы в `supabase/migrations/`
2. Выполнит их в порядке именования
3. Применит к вашей базе данных

Больше не нужно копировать SQL вручную! 🎉

---

**Следующие шаги:**
1. Откройте терминал
2. Выполните `npx supabase login`
3. Следуйте инструкциям выше

