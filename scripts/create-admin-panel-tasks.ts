#!/usr/bin/env tsx
/**
 * Скрипт для создания задач админ панели в Linear
 */

import { config } from 'dotenv';
config({ path: '.env.local' });

import { createIssue, getTeams } from '../src/lib/linear';

interface Task {
  title: string;
  description: string;
  priority: number; // 0 = No priority, 1 = Urgent, 2 = High, 3 = Medium, 4 = Low
  labels?: string[];
}

const tasks: Task[] = [
  // Этап 1: Базовая инфраструктура
  {
    title: '[ADMIN] Создать таблицу users в БД',
    description: `Создать таблицу users для хранения информации о пользователях платформы.

**Требования:**
- Таблица должна содержать: id, email, full_name, role, organization_id, is_active, last_login_at
- Связь с Supabase Auth через auth_user_id
- Индексы на email, role, organization_id
- RLS политики для безопасности

**Файлы:**
- supabase/migrations/011_create_users_table.sql

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → База данных`,
    priority: 2, // High
    labels: ['DB', 'Admin'],
  },
  {
    title: '[ADMIN] Настроить Supabase Auth',
    description: `Настроить Supabase Authentication для авторизации пользователей.

**Требования:**
- Настроить email/password авторизацию
- Создать страницы login и register
- Интеграция с таблицей users
- Обработка ошибок авторизации

**Файлы:**
- src/app/(auth)/login/page.tsx
- src/app/(auth)/register/page.tsx
- src/components/auth/LoginForm.tsx

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Авторизация и роли`,
    priority: 2, // High
    labels: ['Auth', 'Admin'],
  },
  {
    title: '[ADMIN] Создать систему ролей и разрешений',
    description: `Реализовать систему ролей и проверку разрешений.

**Требования:**
- Константы ролей: super_admin, admin, moderator, school_admin, user
- Функция hasPermission() для проверки доступа
- Типы TypeScript для ролей

**Файлы:**
- src/lib/auth/roles.ts
- src/lib/auth/permissions.ts
- src/lib/auth/middleware.ts

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Авторизация и роли`,
    priority: 2, // High
    labels: ['Auth', 'Admin'],
  },
  {
    title: '[ADMIN] Создать middleware для защиты маршрутов',
    description: `Создать Next.js middleware для защиты админ маршрутов.

**Требования:**
- Проверка авторизации для /admin/*
- Проверка роли пользователя
- Редирект на login если не авторизован
- Редирект на главную если нет прав

**Файлы:**
- src/middleware.ts

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Технические детали`,
    priority: 2, // High
    labels: ['Auth', 'Admin'],
  },
  {
    title: '[ADMIN] Создать базовый layout админ панели',
    description: `Создать layout для админ панели с sidebar и header.

**Требования:**
- Sidebar с навигацией
- Header с информацией о пользователе
- Использовать контейнерную систему
- Адаптивный дизайн

**Файлы:**
- src/app/(admin)/layout.tsx
- src/components/admin/AdminSidebar.tsx
- src/components/admin/AdminHeader.tsx

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Архитектура`,
    priority: 2, // High
    labels: ['UI', 'Admin'],
  },

  // Этап 2: Dashboard
  {
    title: '[ADMIN] Создать страницу Dashboard',
    description: `Создать главную страницу админ панели с статистикой.

**Требования:**
- Виджеты статистики: всего школ, активных, пользователей
- Графики роста школ по месяцам
- Activity log последних действий
- Быстрые действия

**Файлы:**
- src/app/(admin)/dashboard/page.tsx
- src/components/admin/DashboardStats.tsx

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Dashboard`,
    priority: 3, // Medium
    labels: ['UI', 'Admin'],
  },

  // Этап 3: Управление школами - Список
  {
    title: '[ADMIN] Создать компонент DataTable',
    description: `Создать переиспользуемый компонент таблицы с сортировкой и фильтрацией.

**Требования:**
- Сортировка по колонкам
- Фильтрация данных
- Пагинация
- Выбор строк (checkbox)
- Использовать @tanstack/react-table

**Файлы:**
- src/components/admin/DataTable.tsx

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Технические детали`,
    priority: 2, // High
    labels: ['UI', 'Admin'],
  },
  {
    title: '[ADMIN] Создать страницу списка школ',
    description: `Создать страницу /admin/schools со списком всех школ.

**Требования:**
- Таблица со всеми школами
- Фильтры: статус, тип, район, верификация
- Поиск по названию
- Массовые действия
- Экспорт в CSV

**Файлы:**
- src/app/(admin)/schools/page.tsx
- src/lib/admin/schools.ts

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Управление школами`,
    priority: 2, // High
    labels: ['UI', 'Admin', 'Schools'],
  },

  // Этап 4: Управление школами - CRUD
  {
    title: '[ADMIN] Создать форму создания школы',
    description: `Создать многошаговую форму для создания новой школы.

**Требования:**
- Многошаговая форма (6 шагов)
- Валидация всех полей (Zod)
- Загрузка изображений (логотип, обложка)
- Предпросмотр перед сохранением

**Файлы:**
- src/app/(admin)/schools/new/page.tsx
- src/components/admin/SchoolForm.tsx

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Создание школы`,
    priority: 2, // High
    labels: ['UI', 'Admin', 'Schools'],
  },
  {
    title: '[ADMIN] Создать форму редактирования школы',
    description: `Создать форму для редактирования существующей школы.

**Требования:**
- Та же форма, но с предзаполненными данными
- История изменений
- Управление филиалами
- Управление изображениями

**Файлы:**
- src/app/(admin)/schools/[id]/page.tsx
- src/components/admin/SchoolForm.tsx (переиспользовать)

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Редактирование школы`,
    priority: 2, // High
    labels: ['UI', 'Admin', 'Schools'],
  },
  {
    title: '[ADMIN] Реализовать удаление школы',
    description: `Добавить функционал удаления школы с подтверждением.

**Требования:**
- Модальное окно подтверждения
- Мягкое удаление (is_active = false) или полное
- Проверка связанных данных (филиалы, отзывы)

**Файлы:**
- Обновить src/lib/admin/schools.ts
- Добавить в DataTable

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Управление школами`,
    priority: 3, // Medium
    labels: ['Admin', 'Schools'],
  },

  // Этап 5: Управление пользователями
  {
    title: '[ADMIN] Создать страницу управления пользователями',
    description: `Создать страницу /admin/users для управления пользователями.

**Требования:**
- Список всех пользователей
- Фильтры по роли, статусу
- Создание/редактирование пользователей
- Управление ролями
- Блокировка пользователей

**Файлы:**
- src/app/(admin)/users/page.tsx
- src/lib/admin/users.ts

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Управление пользователями`,
    priority: 3, // Medium
    labels: ['UI', 'Admin', 'Users'],
  },

  // Этап 6: Модерация и аналитика
  {
    title: '[ADMIN] Создать страницу модерации отзывов',
    description: `Создать страницу для модерации отзывов (когда будет реализована система отзывов).

**Требования:**
- Список отзывов, ожидающих модерации
- Одобрение/отклонение
- Редактирование отзывов

**Файлы:**
- src/app/(admin)/reviews/page.tsx

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Модерация отзывов`,
    priority: 4, // Low (пока нет системы отзывов)
    labels: ['Admin', 'Moderation'],
  },
  {
    title: '[ADMIN] Создать страницу аналитики',
    description: `Создать страницу /admin/analytics с базовой статистикой.

**Требования:**
- Статистика по школам
- Статистика по пользователям
- Популярные школы
- Экспорт отчетов

**Файлы:**
- src/app/(admin)/analytics/page.tsx

**Ссылки:**
- docs/modules/admin/ADMIN_PANEL_PLAN.md → Аналитика`,
    priority: 4, // Low
    labels: ['Admin', 'Analytics'],
  },
];

async function main() {
  console.log('\n🚀 Создание задач админ панели в Linear...\n');

  try {
    // Получаем команду
    const teams = await getTeams();
    if (teams.length === 0) {
      console.error('❌ Команды не найдены в Linear.');
      process.exit(1);
    }

    const team = teams[0]; // Используем первую команду
    console.log(`📁 Команда: ${team.name} (${team.key})\n`);

    // Создаем задачи
    for (const task of tasks) {
      console.log(`📝 Создание: ${task.title}...`);
      
      const issue = await createIssue({
        teamId: team.id,
        title: task.title,
        description: task.description,
        priority: task.priority,
        labels: task.labels || [],
      });

      console.log(`   ✅ Создано: ${issue.identifier} - ${issue.title}\n`);
    }

    console.log('────────────────────────────────────────────────────────────────────────────────\n');
    console.log(`✅ Всего создано задач: ${tasks.length}\n`);
    console.log('📋 Этапы реализации:');
    console.log('   1. Базовая инфраструктура (5 задач)');
    console.log('   2. Dashboard (1 задача)');
    console.log('   3. Управление школами - Список (2 задачи)');
    console.log('   4. Управление школами - CRUD (3 задачи)');
    console.log('   5. Управление пользователями (1 задача)');
    console.log('   6. Модерация и аналитика (2 задачи)');
    console.log('\n🎯 Начните с задач Этапа 1 (базовая инфраструктура)\n');
  } catch (error: any) {
    console.error('\n❌ Ошибка при создании задач:\n');
    console.error('   ', error.message);
    process.exit(1);
  }
}

main().catch(console.error);

