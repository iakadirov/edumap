# Оптимизация изображений для карточек школ

> Дата: Декабрь 2024  
> Цель: Оптимизировать загрузку изображений в карточках школ, используя thumbnail версии

---

## 📋 Обзор

Для улучшения производительности и снижения трафика реализована система автоматического создания thumbnail версий изображений (логотипов и баннеров) при загрузке. В карточках школ используются уменьшенные версии, а на странице школы - полные версии.

---

## ✅ Реализованные функции

### 1. Автоматическое создание thumbnail версий

При загрузке логотипов и баннеров автоматически создаются уменьшенные версии:

- **Логотипы**: 128×128px (WebP, качество 85%)
- **Баннеры**: 640×360px (WebP, качество 85%)
- **Галерея**: 400×400px (WebP, качество 85%)

Thumbnail версии сохраняются с суффиксом `_thumb` в имени файла:
- Оригинал: `logos/123/logo.webp`
- Thumbnail: `logos/123/logo_thumb.webp`

### 2. Использование thumbnail в карточках

Компонент `SchoolCard` автоматически использует thumbnail версии:
- Логотипы в карточках загружают thumbnail версию (128×128px)
- Баннеры в карточках загружают thumbnail версию (640×360px)
- Если thumbnail недоступен, используется оригинальная версия

### 3. Полные версии на странице школы

Компонент `SchoolHero` использует полные версии изображений:
- Логотип: полный размер (80×80px display, но загружается полная версия)
- Баннер: полный размер (до 1440px width)

---

## 📁 Созданные файлы

### Утилиты
- `src/lib/utils/image-thumbnail.ts` - Утилиты для работы с thumbnail ключами
- `src/lib/storage/image-processor.ts` - Обработка изображений и создание thumbnail

### Обновленные файлы
- `src/app/api/upload/route.ts` - Добавлено создание thumbnail при загрузке
- `src/lib/utils/image-url.ts` - Добавлена функция `getThumbnailUrl()`
- `src/components/schools/SchoolCard.tsx` - Использует thumbnail версии
- `src/app/api/storage/url/route.ts` - Поддержка thumbnail ключей

---

## 🔧 Технические детали

### Размеры thumbnail

```typescript
export const THUMBNAIL_SIZES = {
  logo: { width: 128, height: 128 },    // 2x для retina (64px display)
  cover: { width: 640, height: 360 },    // 2x для retina (320px display)
  gallery: { width: 400, height: 400 },  // Для галереи
};
```

### Формат thumbnail

- **Формат**: WebP (лучшее сжатие)
- **Качество**: 85% (баланс между размером и качеством)
- **Режим обрезки**: `cover` (изображение заполняет размеры, центрируется)

### Процесс создания thumbnail

1. При загрузке изображения через `/api/upload`
2. Если тип `logo` или `cover` и это изображение:
   - Создается thumbnail версия с помощью `sharp`
   - Thumbnail загружается в storage с суффиксом `_thumb`
   - Возвращается `thumbnailKey` и `thumbnailUrl` в ответе API

### Использование в компонентах

```typescript
// В SchoolCard - используем thumbnail
const [coverImageUrl, setCoverImageUrl] = useState<string | null>(null);

useEffect(() => {
  if (school.cover_image_url) {
    getThumbnailUrl(school.cover_image_url, true)
      .then((thumbnailUrl) => {
        setCoverImageUrl(thumbnailUrl);
      });
  }
}, [school.cover_image_url]);
```

```typescript
// В SchoolHero - используем полную версию (по умолчанию)
<OptimizedImage
  src={school.coverImage}  // Полная версия
  alt={school.name}
  fill
  priority
/>
```

---

## 📊 Преимущества

### Производительность
- ⚡ Уменьшение размера файлов на 70-90% для thumbnail версий
- ⚡ Быстрее загрузка страниц со списком школ
- ⚡ Меньше трафика для пользователей

### Пользовательский опыт
- ✨ Быстрая загрузка карточек в каталоге
- ✨ Плавная прокрутка без задержек
- ✨ Высокое качество на странице школы (полные версии)

### Масштабируемость
- 🔧 Автоматическое создание thumbnail при загрузке
- 🔧 Обратная совместимость (если thumbnail нет, используется оригинал)
- 🔧 Легко расширяется для других типов изображений

---

## 🚀 Использование

### Загрузка изображения с thumbnail

```typescript
const formData = new FormData();
formData.append('file', file);
formData.append('type', 'logo');
formData.append('organizationId', '123');

const response = await fetch('/api/upload', {
  method: 'POST',
  body: formData,
});

const data = await response.json();
// data.key - ключ оригинального файла
// data.url - URL оригинального файла
// data.thumbnailKey - ключ thumbnail версии (если создана)
// data.thumbnailUrl - URL thumbnail версии (если создана)
```

### Получение thumbnail URL

```typescript
import { getThumbnailUrl } from '@/lib/utils/image-url';

// Получить thumbnail URL из оригинального URL
const thumbnailUrl = await getThumbnailUrl(originalUrl, true);

// Использовать оригинальный URL (не thumbnail)
const originalUrl = await getThumbnailUrl(originalUrl, false);
```

---

## 🔄 Обратная совместимость

Система полностью обратно совместима:
- Если thumbnail версия не существует, используется оригинальная версия
- Старые изображения (загруженные до внедрения) продолжают работать
- Можно вручную создать thumbnail для существующих изображений

### ⚠️ Важно: Старые изображения

**Старые изображения (загруженные до внедрения thumbnail системы) будут отображаться в полном размере в карточках**, так как для них нет thumbnail версий.

Для оптимизации старых изображений используйте скрипт генерации thumbnail:

```bash
# Генерация thumbnail для всех изображений из БД
npx tsx scripts/generate-thumbnails.ts

# Генерация thumbnail только для логотипов
npx tsx scripts/generate-thumbnails.ts --prefix logos/

# Генерация thumbnail только для баннеров
npx tsx scripts/generate-thumbnails.ts --prefix covers/

# Режим dry-run (проверка без создания)
npx tsx scripts/generate-thumbnails.ts --dry-run
```

Скрипт:
- Получает список изображений из БД или storage
- Проверяет, существует ли уже thumbnail
- Скачивает оригинальное изображение
- Создает thumbnail версию
- Загружает thumbnail в storage

---

## 📝 Следующие шаги (опционально)

1. **Миграция существующих изображений**
   - Создать скрипт для генерации thumbnail для существующих изображений
   - Запустить в фоновом режиме

2. **Дополнительные оптимизации**
   - Lazy loading для изображений в карточках
   - Progressive image loading (blur placeholder)
   - Кэширование thumbnail URLs

3. **Мониторинг**
   - Отслеживание размера файлов
   - Аналитика использования thumbnail vs оригиналов
   - Мониторинг производительности загрузки

---

## 📚 Связанные документы

- [Схема базы данных](./modules/schools/DATABASE_SCHEMA.md)
- [API документация](./API.md)
- [Лог прогресса](./PROGRESS_LOG.md)

---

*Документация обновлена: Декабрь 2024*

