# Сравнение схем данных — Текущая vs DATA_SCHEMA.md

> Анализ различий между текущей схемой БД и полным описанием в DATA_SCHEMA.md

---

## ✅ Уже реализовано

### organizations (базовая информация)
- ✅ `name`, `slug`, `description`
- ✅ `logo_url`
- ✅ `phone`, `email`, `website`
- ✅ `address`, `district`, `city`
- ✅ `lat`, `lng`
- ✅ `status`, `is_verified`
- ✅ `overall_rating`, `reviews_count`
- ✅ `parent_organization_id` (филиалы)

### school_details (детали школы)
- ✅ `school_type`
- ✅ `accepts_preparatory` (0 класс)
- ✅ `grade_from`, `grade_to`
- ✅ `total_students`, `avg_class_size`
- ✅ `primary_language`, `additional_languages`
- ✅ `curriculum`
- ✅ `fee_monthly_min`, `fee_monthly_max`
- ✅ `has_transport`, `has_meals`, `has_extended_day`

---

## 🔶 Приоритетные улучшения (Phase 2)

### 1. organizations — Расширение базовой информации

#### Многоязычность
- [ ] `name_uz` TEXT — название на узбекском
- [ ] `name_ru` TEXT — название на русском
- [ ] `short_description` TEXT — краткое описание (до 160 символов)

#### Дополнительная информация
- [ ] `cover_image_url` TEXT — обложка школы
- [ ] `founded_year` INTEGER — год основания
- [ ] `motto` TEXT — девиз школы

#### Улучшение локации
- [ ] `region` TEXT — область (Ташкент, Андижан, и т.д.)
- [ ] `landmark` TEXT — ориентир ("Рядом с метро Космонавтов")
- [ ] `google_maps_url` TEXT — ссылка на Google Maps
- [ ] `yandex_maps_url` TEXT — ссылка на Яндекс Карты

#### Расширение контактов
- [ ] `phone_primary` TEXT — основной телефон (переименовать `phone`)
- [ ] `phone_secondary` TEXT — дополнительный телефон
- [ ] `phone_admission` TEXT — телефон приёмной комиссии
- [ ] `email_admission` TEXT — email приёмной комиссии
- [ ] `telegram` TEXT — Telegram контакт

#### Социальные сети
- [ ] `instagram` TEXT — Instagram
- [ ] `facebook` TEXT — Facebook
- [ ] `telegram_channel` TEXT — Telegram канал
- [ ] `youtube` TEXT — YouTube
- [ ] `linkedin` TEXT — LinkedIn
- [ ] `tiktok` TEXT — TikTok

---

### 2. school_details — Расширение деталей

#### Расписание и режим работы
- [ ] `school_start_time` TIME — время начала занятий (08:30)
- [ ] `school_end_time` TIME — время окончания (15:00)
- [ ] `lesson_duration` INTEGER — длительность урока в минутах (45)
- [ ] `lessons_per_day` INTEGER — количество уроков в день
- [ ] `has_saturday_classes` BOOLEAN — занятия в субботу
- [ ] `extended_day_until` TIME — продленка до (18:00)

#### Педагогический состав (базовые метрики)
- [ ] `total_teachers` INTEGER — общее количество учителей
- [ ] `teachers_with_higher_ed_percent` DECIMAL(5,2) — % с высшим образованием
- [ ] `avg_teacher_experience_years` DECIMAL(4,1) — средний стаж (лет)
- [ ] `has_foreign_teachers` BOOLEAN — есть иностранные преподаватели
- [ ] `native_english_speakers_count` INTEGER — носители английского

#### Инфраструктура (основные объекты)
- [ ] `school_area_sqm` DECIMAL(10,2) — площадь школы (м²)
- [ ] `classrooms_count` INTEGER — количество кабинетов
- [ ] `has_gym` BOOLEAN — спортзал
- [ ] `has_swimming_pool` BOOLEAN — бассейн
- [ ] `has_football_field` BOOLEAN — футбольное поле
- [ ] `has_library` BOOLEAN — библиотека
- [ ] `has_computer_lab` BOOLEAN — компьютерный класс
- [ ] `has_science_labs` BOOLEAN — лаборатории
- [ ] `has_medical_room` BOOLEAN — медпункт
- [ ] `has_cafeteria` BOOLEAN — столовая

#### Безопасность
- [ ] `has_security_24_7` BOOLEAN — охрана 24/7
- [ ] `has_cctv` BOOLEAN — видеонаблюдение
- [ ] `has_psychologist` BOOLEAN — психолог

#### Финансовые детали
- [ ] `entrance_fee` DECIMAL(10,2) — вступительный взнос
- [ ] `textbook_fee_yearly` DECIMAL(10,2) — оплата за учебники (в год)
- [ ] `uniform_fee` DECIMAL(10,2) — стоимость формы
- [ ] `meal_fee_monthly` DECIMAL(10,2) — стоимость питания (в месяц)
- [ ] `transport_fee_monthly` DECIMAL(10,2) — стоимость транспорта (в месяц)
- [ ] `has_sibling_discount` BOOLEAN — скидка для второго ребёнка
- [ ] `sibling_discount_percent` INTEGER — процент скидки

#### Документы и лицензии
- [ ] `license_number` TEXT — номер лицензии
- [ ] `license_date` DATE — дата выдачи лицензии
- [ ] `license_valid_until` DATE — срок действия лицензии
- [ ] `license_authority` TEXT — орган выдачи лицензии
- [ ] `has_international_accreditation` BOOLEAN — международная аккредитация
- [ ] `accreditation_body` TEXT — орган аккредитации

#### Дополнительное образование
- [ ] `extracurricular_activities` TEXT[] — кружки и секции (массив)
- [ ] `clubs` TEXT[] — клубы
- [ ] `sports_sections` TEXT[] — спортивные секции

---

## 🔵 Желательные улучшения (Phase 3)

### Результаты и достижения
- `university_admission_rate` DECIMAL(5,2) — % поступления в вузы
- `olympiad_wins_count` INTEGER — количество побед на олимпиадах
- `avg_ielts_score` DECIMAL(3,1) — средний балл IELTS
- `scholarship_recipients_yearly` INTEGER — стипендиаты в год

### Расширенная инфраструктура
- `building_year` INTEGER — год постройки здания
- `floors_count` INTEGER — количество этажей
- `gym_area_sqm` DECIMAL(10,2) — площадь спортзала
- `library_books_count` INTEGER — количество книг в библиотеке
- `computer_lab_stations` INTEGER — компьютеров в классе

### Медиа контент
- `photos` JSONB — фотографии (массив объектов)
- `video_tour_url` TEXT — видео тур
- `promo_video_url` TEXT — промо видео
- `has_virtual_tour` BOOLEAN — виртуальный тур

### Поступление
- `admission_status` ENUM — статус приёма (open/closed/waitlist)
- `admission_period` TEXT — период приёма
- `has_entrance_test` BOOLEAN — вступительный тест
- `has_interview` BOOLEAN — собеседование
- `required_documents` TEXT[] — необходимые документы

---

## 📋 Рекомендуемый план действий

### Шаг 1: Минимальные улучшения (сейчас)
1. Добавить `region` в `organizations` — важно для фильтрации
2. Добавить `short_description` — для карточек школ
3. Добавить `founded_year` — полезная информация
4. Добавить социальные сети (instagram, telegram_channel, youtube) — важны для контактов

### Шаг 2: Контакты и расписание (Phase 2.1)
1. Расширить контакты: `phone_admission`, `email_admission`, `telegram`
2. Добавить расписание: `school_start_time`, `school_end_time`, `lesson_duration`
3. Добавить `extended_day_until`

### Шаг 3: Инфраструктура и финансы (Phase 2.2)
1. Добавить основные объекты инфраструктуры (10 полей)
2. Добавить финансовые детали (entrance_fee, meal_fee, transport_fee)
3. Добавить безопасность (security, cctv, psychologist)

### Шаг 4: Документы и педагоги (Phase 2.3)
1. Добавить лицензии (license_number, license_date, etc.)
2. Добавить базовые метрики педагогов
3. Добавить дополнительное образование (extracurricular_activities)

---

## 💡 Важные заметки

1. **Постепенность**: Не все поля обязательны — могут быть NULL
2. **Валидация**: Добавить CHECK constraints где нужно
3. **Индексы**: Добавить индексы для часто используемых полей
4. **Обратная совместимость**: Новые поля должны быть nullable
5. **UI**: Показывать только заполненные поля в интерфейсе

---

*Обновлено: 12 декабря 2025*

