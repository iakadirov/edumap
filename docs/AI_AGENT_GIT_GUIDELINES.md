# Инструкции для ИИ агентов: Работа с Git и деплоями

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА

### 1. Группировка изменений в коммиты

**НЕ ДЕЛАТЬ:**
- ❌ Делать отдельный коммит и push для каждого маленького изменения
- ❌ Пушить после каждого исправления опечатки или форматирования
- ❌ Создавать коммиты типа "fix typo", "add space", "update comment"

**ДЕЛАТЬ:**
- ✅ Группировать связанные изменения в один коммит
- ✅ Делать один push после завершения логически завершенной задачи
- ✅ Объединять несколько мелких исправлений в один коммит

### 2. Когда делать коммит и push

**Делать коммит и push ТОЛЬКО когда:**
1. ✅ Завершена логически завершенная задача (например, "создана страница X со всеми компонентами")
2. ✅ Исправлена критическая ошибка, которая блокирует работу
3. ✅ Завершен этап работы, который можно протестировать отдельно
4. ✅ Пользователь явно просит сделать push

**НЕ делать коммит и push:**
1. ❌ После каждого маленького изменения
2. ❌ После исправления опечаток или форматирования
3. ❌ Если есть незавершенные изменения, которые логически связаны
4. ❌ Если планируются еще изменения в том же файле/функциональности

### 3. Примеры правильной группировки

**Правильно:**
```bash
# Один коммит для всей функциональности
git add src/app/admin/schools/[id]/infrastructure/page.tsx \
        src/app/admin/schools/[id]/services/page.tsx \
        src/app/admin/schools/[id]/admission/page.tsx \
        src/app/api/admin/schools/[id]/results/route.ts
git commit -m "feat: Create missing school section pages and results API endpoint"
git push
```

**Неправильно:**
```bash
# Множество маленьких коммитов
git add src/app/admin/schools/[id]/infrastructure/page.tsx
git commit -m "feat: Add infrastructure page"
git push

git add src/app/admin/schools/[id]/services/page.tsx
git commit -m "feat: Add services page"
git push

git add src/app/admin/schools/[id]/admission/page.tsx
git commit -m "feat: Add admission page"
git push
```

### 4. Структура коммитов

**Хорошие сообщения коммитов:**
- `feat: Add school infrastructure, services, and admission pages`
- `fix: Resolve TypeScript errors in school creation wizard`
- `refactor: Improve error handling in API routes`
- `docs: Update API documentation`

**Плохие сообщения коммитов:**
- `fix typo`
- `update`
- `changes`
- `fix`

### 5. Работа с несколькими файлами

**Если нужно изменить несколько файлов для одной задачи:**

1. **Сначала внесите все изменения** во все файлы
2. **Проверьте, что все изменения логически связаны**
3. **Сделайте один коммит** со всеми изменениями
4. **Сделайте один push**

**Пример:**
```bash
# Изменения в нескольких файлах для одной задачи
# 1. Внесите все изменения
# 2. Проверьте работоспособность
# 3. Один коммит
git add file1.ts file2.ts file3.ts
git commit -m "feat: Implement user authentication flow with login, logout, and session management"
git push
```

### 6. Исключения (когда можно делать отдельные коммиты)

**Можно делать отдельные коммиты только для:**
1. 🔴 Критические исправления безопасности
2. 🔴 Исправления, которые блокируют работу всего приложения
3. 🔴 Изменения, которые требуют немедленного деплоя по запросу пользователя

### 7. Проверка перед push

**Перед каждым push проверьте:**
1. ✅ Все связанные изменения включены в коммит
2. ✅ Код компилируется без ошибок
3. ✅ Нет очевидных ошибок в коде
4. ✅ Сообщение коммита описывает все изменения

### 8. Работа с Vercel и rate limiting

**Проблема:**
- Vercel имеет лимиты на количество деплоев
- Частые коммиты и пуши приводят к rate limiting
- Это блокирует деплои на несколько часов

**Решение:**
- Группировать изменения в логические коммиты
- Делать push только после завершения задачи
- Избегать множественных пушей для одной задачи

### 9. Пример рабочего процесса

**Правильный процесс:**

1. Пользователь просит: "Создай страницы для infrastructure, services, admission"
2. Агент создает все три страницы
3. Агент проверяет, что все работает
4. Агент делает **один коммит** со всеми тремя страницами
5. Агент делает **один push**

**Неправильный процесс:**

1. Пользователь просит: "Создай страницы для infrastructure, services, admission"
2. Агент создает страницу infrastructure → коммит → push
3. Агент создает страницу services → коммит → push
4. Агент создает страницу admission → коммит → push

### 10. Команды для проверки перед push

```bash
# Проверить статус
git status

# Посмотреть, что будет в коммите
git diff --staged

# Убедиться, что все связанные файлы добавлены
git status --short
```

## 📋 Чеклист перед push

- [ ] Все логически связанные изменения включены в коммит
- [ ] Код компилируется без ошибок
- [ ] Сообщение коммита описывает все изменения
- [ ] Нет незавершенных изменений, которые должны быть в этом коммите
- [ ] Пользователь не просил сделать push раньше времени

## 🎯 Цель

**Минимизировать количество коммитов и пушей, группируя логически связанные изменения.**

---

*Последнее обновление: Декабрь 2025*

