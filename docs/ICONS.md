# Иконки в проекте

В проекте используются два пакета иконок для максимальной гибкости:

## 📦 Установленные пакеты

1. **lucide-react** - основной пакет иконок (уже установлен)
2. **@solar-icons/react-perf** - Solar Icons (performance версия, оптимизированная для bundle size)

## 🎨 Solar Icons

### Установка

Пакет уже установлен:
```bash
npm install @solar-icons/react-perf
```

### Использование

Solar Icons предоставляет 6 стилей иконок. В performance версии каждый компонент включает только один стиль для оптимизации размера бандла.

#### Базовое использование

```tsx
import { Home, User, Settings } from '@solar-icons/react-perf';

function MyComponent() {
  return (
    <div>
      <Home size={24} />
      <User size={20} color="#000" />
      <Settings size={24} />
    </div>
  );
}
```

#### Примеры стилей

Solar Icons поддерживает 6 стилей:
- `Bold` - жирные иконки
- `Broken` - разбитые линии
- `Bulk` - заливка
- `Linear` - линейные
- `Outline` - контурные (по умолчанию)
- `TwoTone` - двухцветные

**Важно:** В performance версии (`@solar-icons/react-perf`) каждый стиль - это отдельный компонент. Если нужен другой стиль, используйте соответствующий суффикс или установите стандартный пакет `@solar-icons/react` для динамической смены стилей.

#### Пример с разными стилями (если используете стандартный пакет)

```tsx
// Только для @solar-icons/react (не perf версии)
import { Home } from '@solar-icons/react';

function MyComponent() {
  return (
    <div>
      <Home variant="linear" size={24} />
      <Home variant="bold" size={24} />
      <Home variant="bulk" size={24} />
    </div>
  );
}
```

### Свойства компонентов

```tsx
interface IconProps {
  size?: number | string;        // Размер иконки (по умолчанию 24)
  color?: string;                 // Цвет иконки (по умолчанию "currentColor")
  className?: string;             // CSS классы
  style?: React.CSSProperties;    // Inline стили
}
```

### Примеры использования в компонентах

```tsx
import { Home, User, Settings, Bell } from '@solar-icons/react-perf';

// В кнопке
<Button>
  <Home size={16} className="mr-2" />
  Главная
</Button>

// В навигации
<nav>
  <Link href="/dashboard">
    <Home size={20} />
    Dashboard
  </Link>
</nav>

// С Tailwind классами
<Home size={24} className="text-primary hover:text-primary/80 transition-colors" />

// В списке
<ul>
  <li>
    <User size={20} className="text-muted-foreground" />
    Профиль
  </li>
</ul>
```

## 🔍 Lucide React

Lucide React продолжает использоваться как основной пакет иконок в shadcn/ui компонентах.

### Пример

```tsx
import { Search, Menu, X } from 'lucide-react';

<Search size={20} />
```

## 📚 Документация

- **Solar Icons**: https://solar-icons.vercel.app/
- **Lucide Icons**: https://lucide.dev/

## 💡 Рекомендации

1. **Solar Icons** - используйте для новых компонентов, где нужен современный дизайн
2. **Lucide React** - продолжайте использовать в существующих компонентах shadcn/ui
3. **Performance** - используем `@solar-icons/react-perf` для меньшего размера бандла
4. **Консистентность** - старайтесь использовать один стиль иконок в рамках одного компонента

## 🎯 Примеры замены в админ-панели

Можно заменить существующие иконки на Solar Icons:

```tsx
// Было (lucide-react)
import { Home, Users, School, Settings } from 'lucide-react';

// Станет (solar-icons)
import { Home, Users, School, Settings } from '@solar-icons/react-perf';
```

**Важно:** Перед заменой убедитесь, что все иконки доступны в Solar Icons. Проверьте на https://solar-icons.vercel.app/

