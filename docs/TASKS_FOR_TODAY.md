# Задачи на сегодня

Список задач для создания в Linear на основе текущего спринта.

> **Примечание:** Используйте скрипт `npm run linear:create-today` для автоматического создания задач. Скрипт использует текущую дату автоматически.

## 📋 Задачи для создания

### Приоритет High (3)

#### [DB] Создать проект в Supabase
```
Создать новый проект в Supabase для EduMap.uz.

Требования:
- Название проекта: EduMap
- Регион: ближайший к Узбекистану
- Настроить базу данных PostgreSQL

После создания сохранить URL и API ключи в .env.local
```

#### [DB] Спроектировать таблицы organizations и school_details
```
Создать схему базы данных для модуля школ.

Таблицы:
1. organizations (общая таблица)
   - id, org_type, name, slug
   - description, logo_url
   - phone, email, website
   - address, district, city, lat, lng
   - status, is_verified
   - overall_rating, reviews_count

2. school_details (специфичная для школ)
   - school_type (private, state, international)
   - grade_from, grade_to
   - total_students, avg_class_size
   - primary_language, additional_languages
   - curriculum (national, cambridge, ib)
   - fee_monthly_min, fee_monthly_max
   - has_transport, has_meals, has_extended_day

См. документацию: docs/modules/schools/README.md
```

#### [SETUP] Настроить Supabase клиент в проекте
```
Настроить подключение к Supabase в Next.js проекте.

Требования:
- Установить @supabase/supabase-js
- Создать клиент в src/lib/supabase/client.ts
- Настроить переменные окружения
- Создать типы TypeScript для таблиц
- Добавить helper функции для работы с БД
```

### Приоритет Medium (2)

#### [DB] Создать первую миграцию для таблиц школ
```
Создать SQL миграцию для создания таблиц organizations и school_details.

Требования:
- Использовать Supabase migrations
- Добавить индексы для поиска
- Настроить foreign keys
- Добавить constraints для валидации данных

Файл: supabase/migrations/001_create_organizations_and_schools.sql
```

#### [UI] Создать базовый Layout компонент
```
Создать основной Layout компонент для приложения.

Требования:
- Обертка для всех страниц
- Поддержка темной/светлой темы (опционально)
- Responsive дизайн
- Использовать shadcn/ui компоненты

Файл: src/components/shared/Layout.tsx
```

## 🚀 Как создать задачи

### Способ 1: Автоматически через скрипт

1. Добавьте API ключ в `.env.local`:
   ```bash
   LINEAR_API_KEY=lin_api_ваш_ключ
   ```

2. Запустите скрипт:
   ```bash
   npx tsx scripts/create-today-tasks.ts
   ```

### Способ 2: Вручную в Linear

1. Откройте Linear: https://linear.app
2. Создайте задачу для каждого пункта выше
3. Используйте формат заголовка: `[МЕТКА] Название`
4. Скопируйте описание из соответствующих задач

### Способ 3: Назначить на Cursor

После создания задач, назначьте их на **Cursor** для автоматической работы:
- В поле Assignee выберите "Cursor"
- Или упомяните `@Cursor` в описании/комментарии

## 📊 Прогресс

- [ ] [DB] Создать проект в Supabase
- [ ] [DB] Спроектировать таблицы
- [ ] [DB] Создать первую миграцию
- [ ] [SETUP] Настроить Supabase клиент
- [ ] [UI] Создать базовый Layout компонент

---

*Документ обновляется автоматически при запуске скрипта создания задач*

