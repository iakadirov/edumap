# Changelog — 17 декабря 2025

## Обзор

В этот день была проведена большая работа по улучшению функциональности админ-панели, исправлению ошибок и оптимизации пользовательского опыта. Все изменения были протестированы и успешно задеплоены.

---

## 🎯 Основные задачи

### 1. Маскирование номеров телефонов

**Проблема:** Номера телефонов вводились в разных форматах, что создавало проблемы с валидацией и отображением.

**Решение:**
- Создан компонент `PhoneInput` (`src/components/ui/phone-input.tsx`)
- Созданы утилиты для нормализации телефонов (`src/lib/utils/phone.ts`)
- Создан компонент `PhoneLink` для отображения кликабельных номеров (`src/components/shared/PhoneLink.tsx`)

**Функциональность:**
- Автоматическое добавление `+` если его нет
- Удаление дефисов и пробелов при вводе
- Форматирование для отображения: `+99890 123 45 67`
- Сохранение в БД в формате: `+998901234567`
- Кликабельные ссылки с `tel:` протоколом

**Файлы:**
- `src/components/ui/phone-input.tsx` (новый)
- `src/lib/utils/phone.ts` (новый)
- `src/components/shared/PhoneLink.tsx` (новый)
- Обновлены все формы: `SchoolCreationForm`, `BasicInfoForm`, `BrandForm`

---

### 2. Форматирование веб-сайтов

**Проблема:** Веб-сайты вводились без протокола `https://`, что создавало проблемы с отображением.

**Решение:**
- Создана утилита `normalizeWebsite` (`src/lib/utils/website.ts`)
- Автоматическое добавление `https://` если протокол отсутствует
- Сохранение полного URL в БД

**Функциональность:**
- `sayt.uz` → `https://sayt.uz`
- `https://sayt.uz` → остается как есть
- `www.sayt.uz` → `https://www.sayt.uz`

**Файлы:**
- `src/lib/utils/website.ts` (новый)
- Обновлены формы создания и редактирования школ

---

### 3. Нормализация YouTube

**Проблема:** YouTube ссылки вводились в разных форматах (username, @username, полный URL).

**Решение:**
- Обновлена функция `normalizeYouTube` в `src/lib/utils/social-media.ts`
- Всегда сохраняется в формате `https://youtube.com/@username`

**Функциональность:**
- `username` → `https://youtube.com/@username`
- `@username` → `https://youtube.com/@username`
- `https://youtube.com/channel/UC...` → `https://youtube.com/@username` (если возможно)

**Файлы:**
- `src/lib/utils/social-media.ts` (обновлен)

---

### 4. Facebook — гибкая обработка URL

**Проблема:** Не все страницы Facebook имеют username, некоторые используют ID профиля.

**Решение:**
- Обновлена функция `normalizeFacebook` для принятия любого валидного URL Facebook
- Поддержка `profile.php?id=...` и других форматов

**Функциональность:**
- Принимает любой валидный URL Facebook как есть
- Не валидирует строго, сохраняет URL в исходном виде
- Поддержка username и ID профилей

**Файлы:**
- `src/lib/utils/social-media.ts` (обновлен)

---

### 5. Страница бренда

**Проблема:** Страница бренда была недоступна по ожидаемому URL.

**Решение:**
- Создан новый роут `/schools/brands/[slug]` (`src/app/schools/brands/[slug]/page.tsx`)
- Старый роут `/brands/[slug]` теперь редиректит на новый
- Обновлен компонент `BrandPage` для корректного отображения

**Функциональность:**
- Доступ по URL: `edumap.uz/schools/brands/registon`
- Отображение информации о бренде
- Список связанных школ в виде карточек
- Социальные сети и контакты

**Файлы:**
- `src/app/schools/brands/[slug]/page.tsx` (новый)
- `src/app/brands/[slug]/page.tsx` (обновлен — редирект)
- `src/components/schools/BrandPage.tsx` (обновлен)

---

### 6. Редактирование slug для школ

**Проблема:** После создания школы нельзя было изменить slug (URL), что создавало проблемы при опечатках.

**Решение:**
- Добавлено поле slug в форму редактирования базовой информации
- Автоматическая генерация slug из названия школы
- Возможность ручного редактирования

**Функциональность:**
- Автогенерация slug при изменении названия
- Ручное редактирование slug
- Предпросмотр URL: `edumap.uz/schools/{slug}`

**Файлы:**
- `src/components/admin/schools/sections/BasicInfoForm.tsx` (обновлен)

---

### 7. Поиск похожих школ

**Проблема:** При создании новой школы можно было случайно создать дубликат.

**Решение:**
- Создан API endpoint `/api/admin/schools/similar` (`src/app/api/admin/schools/similar/route.ts`)
- Создан компонент `SimilarSchoolsSearch` (`src/components/admin/schools/SimilarSchoolsSearch.tsx`)
- Интегрирован в форму создания школы

**Функциональность:**
- Поиск школ с 80%+ схожестью по названию
- Алгоритм Левенштейна для вычисления схожести
- Отображение похожих школ с логотипами
- Кликабельные ссылки на похожие школы (открываются в новой вкладке)
- Debounce 500ms для оптимизации запросов

**Файлы:**
- `src/app/api/admin/schools/similar/route.ts` (новый)
- `src/components/admin/schools/SimilarSchoolsSearch.tsx` (новый)
- `src/components/admin/schools/SchoolCreationForm.tsx` (обновлен)

---

### 8. Замена основного цвета

**Задача:** Изменить основной синий цвет на `#0077ff`.

**Решение:**
- Обновлен файл `src/app/globals.css`
- Заменены все вхождения старого цвета на новый

**Изменения:**
- `--primary: oklch(0.55 0.2 264)` (соответствует #0077ff)
- `--ring: oklch(0.55 0.2 264)`
- `--accent-foreground: oklch(0.55 0.2 264)`
- `--sidebar-primary: oklch(0.55 0.2 264)`

**Файлы:**
- `src/app/globals.css` (обновлен)

---

### 9. Исправления с изображениями

**Проблема:** Множественные ошибки "Failed to parse src" при использовании storage ключей вместо полных URL.

**Решение:**
- Заменен `Image` из `next/image` на `OptimizedImage` во всех компонентах
- `OptimizedImage` автоматически обрабатывает storage ключи и получает presigned URLs

**Исправленные компоненты:**
- `BrandList.tsx`
- `BrandSearch.tsx`
- `BrandPage.tsx`
- `SimilarSchoolsSearch.tsx`

**Функциональность:**
- Автоматическое определение storage ключей
- Получение presigned URLs через `/api/storage/url`
- Обработка истекших URLs
- Fallback для ошибок загрузки

**Файлы:**
- `src/components/admin/brands/BrandList.tsx` (обновлен)
- `src/components/admin/brands/BrandSearch.tsx` (обновлен)
- `src/components/schools/BrandPage.tsx` (обновлен)
- `src/components/admin/schools/SimilarSchoolsSearch.tsx` (обновлен)

---

### 10. Исправление Yandex Maps

**Проблема:** Ошибка "api is already enabled on this page with same namespace" — скрипт загружался дважды.

**Решение:**
- Добавлена проверка на существующий скрипт перед добавлением нового
- Если скрипт уже загружается, компонент ждет его загрузки

**Функциональность:**
- Проверка `window.ymaps` перед загрузкой
- Проверка существующего script элемента
- Ожидание загрузки существующего скрипта

**Файлы:**
- `src/components/admin/schools/YandexMap.tsx` (обновлен)

---

### 11. Улучшения UI/UX

#### Кнопки "Посмотреть на сайте"

**Добавлены кнопки просмотра:**
- На странице редактирования бренда (`/admin/brands/[id]`)
- В таблице школ (`/admin/schools`) — мини-кнопка с иконкой
- В таблице брендов (`/admin/brands`) — пункт в dropdown меню

**Файлы:**
- `src/app/admin/brands/[id]/page.tsx` (обновлен)
- `src/components/admin/schools/SchoolsTable.tsx` (обновлен)
- `src/components/admin/brands/BrandList.tsx` (обновлен)

#### Упрощение таблицы брендов

**Изменения:**
- Убрана колонка "Slug"
- Убрана колонка "Veb-sayt"
- Оставлены: Brend, Maktablar, Yaratilgan, Harakatlar

**Файлы:**
- `src/components/admin/brands/BrandList.tsx` (обновлен)

---

## 🐛 Исправленные ошибки

### Ошибки деплоя

1. **TypeScript ошибки:**
   - Исправлен тип параметра `price` в `BasicInfoForm.tsx`
   - Разделены `fetchingRef` на `fetchingKeyRef` и `fetchingPresignedRef` в `optimized-image.tsx`

2. **Runtime ошибки:**
   - Исправлена ошибка "Failed to construct 'URL': Invalid URL" в API endpoints
   - Исправлена ошибка "Failed to parse src" для storage ключей
   - Исправлена двойная загрузка Yandex Maps скрипта

3. **Ошибки валидации:**
   - Улучшена обработка ошибок в API endpoints
   - Добавлены try-catch блоки для безопасной обработки

---

## 📊 Статистика изменений

**Коммит:** `e023f5b`  
**Дата:** 17 декабря 2025

**Изменения:**
- 20 файлов изменено
- 7 новых файлов создано
- 13 файлов обновлено
- 805 строк добавлено
- 207 строк удалено

**Новые файлы:**
1. `src/app/api/admin/schools/similar/route.ts`
2. `src/app/schools/brands/[slug]/page.tsx`
3. `src/components/admin/schools/SimilarSchoolsSearch.tsx`
4. `src/components/shared/PhoneLink.tsx`
5. `src/components/ui/phone-input.tsx`
6. `src/lib/utils/phone.ts`
7. `src/lib/utils/website.ts`

---

## ✅ Тестирование

Все изменения были протестированы:
- ✅ TypeScript компиляция без ошибок
- ✅ Линтер не нашел ошибок
- ✅ Локальное тестирование функциональности
- ✅ Проверка на ошибки деплоя

---

## 🚀 Деплой

Изменения успешно задеплоены на Vercel:
- Коммит: `e023f5b`
- Ветка: `main`
- Статус: ✅ Успешно

---

## 📝 Примечания

1. **Маскирование телефонов:** Теперь работает только с `+`, не добавляет `998` автоматически, что позволяет использовать номера других стран.

2. **Facebook:** Принимает любой URL как есть, что важно для страниц без username.

3. **Изображения:** Все компоненты теперь используют `OptimizedImage`, который автоматически обрабатывает storage ключи.

4. **Yandex Maps:** Исправлена проблема с двойной загрузкой скрипта, что устраняет предупреждения в консоли.

5. **Поиск похожих школ:** Помогает избежать создания дубликатов при добавлении новых школ.

---

## 🔄 Следующие шаги (рекомендации)

1. Добавить тесты для новых утилит (phone, website, social-media)
2. Рассмотреть возможность кэширования результатов поиска похожих школ
3. Добавить метрики для отслеживания использования новых функций
4. Оптимизировать алгоритм поиска похожих школ для больших объемов данных

---

**Документация создана:** 17 декабря 2025  
**Автор:** AI Assistant  
**Версия:** 1.0

