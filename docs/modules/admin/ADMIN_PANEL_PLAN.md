# План реализации админ панели — EduMap.uz

> **Цель**: Создать полноценную админ панель для управления платформой
> 
> **Приоритет**: High
> **Спринт**: 2 — Admin Panel MVP
> **Дата начала**: Декабрь 2025

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [Архитектура](#архитектура)
3. [База данных](#база-данных)
4. [Авторизация и роли](#авторизация-и-роли)
5. [Функциональность админ панели](#функциональность-админ-панели)
6. [Этапы реализации](#этапы-реализации)
7. [Технические детали](#технические-детали)

---

## 🎯 Обзор

### Цели
- Управление школами (CRUD операции)
- Управление пользователями
- Модерация контента
- Аналитика и статистика
- Настройки платформы

### Пользователи
1. **Super Admin** — полный доступ ко всему
2. **Admin** — управление школами и модерация
3. **Moderator** — только модерация контента
4. **School Admin** — управление своей школой (отдельный модуль)

---

## 🏗 Архитектура

### Структура папок

```
src/
├── app/
│   ├── (auth)/                    # Страницы авторизации
│   │   ├── login/
│   │   ├── register/
│   │   └── layout.tsx
│   │
│   ├── (admin)/                   # Админ панель (защищено)
│   │   ├── layout.tsx             # Admin layout с sidebar
│   │   ├── dashboard/             # Главная страница
│   │   ├── schools/               # Управление школами
│   │   │   ├── page.tsx           # Список школ
│   │   │   ├── new/               # Создание школы
│   │   │   └── [id]/              # Редактирование школы
│   │   ├── users/                 # Управление пользователями
│   │   ├── reviews/                # Модерация отзывов
│   │   ├── analytics/             # Аналитика
│   │   └── settings/              # Настройки платформы
│   │
│   └── (main)/                    # Публичные страницы
│
├── components/
│   ├── admin/                     # Компоненты админ панели
│   │   ├── AdminSidebar.tsx
│   │   ├── AdminHeader.tsx
│   │   ├── DataTable.tsx          # Таблица с сортировкой/фильтрацией
│   │   ├── SchoolForm.tsx          # Форма создания/редактирования школы
│   │   └── UserManagement.tsx
│   │
│   └── auth/                      # Компоненты авторизации
│       ├── LoginForm.tsx
│       └── ProtectedRoute.tsx
│
└── lib/
    ├── auth/                      # Логика авторизации
    │   ├── middleware.ts          # Проверка ролей
    │   ├── roles.ts               # Константы ролей
    │   └── permissions.ts         # Система разрешений
    │
    └── admin/                     # API для админ панели
        ├── schools.ts             # CRUD операции для школ
        └── users.ts               # Управление пользователями
```

---

## 💾 База данных

### Новая таблица: `users`

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE NOT NULL,
  full_name TEXT,
  role TEXT NOT NULL DEFAULT 'user' CHECK (role IN ('super_admin', 'admin', 'moderator', 'school_admin', 'user')),
  organization_id UUID REFERENCES organizations(id) ON DELETE SET NULL, -- Для school_admin
  is_active BOOLEAN NOT NULL DEFAULT true,
  last_login_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Индексы
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_role ON users(role);
CREATE INDEX idx_users_organization_id ON users(organization_id);

-- Связь с Supabase Auth
-- Используем auth.users.id как внешний ключ
ALTER TABLE users ADD COLUMN auth_user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE;
CREATE UNIQUE INDEX idx_users_auth_user_id ON users(auth_user_id);
```

### Обновление таблицы `organizations`

```sql
-- Добавить поле для связи с пользователем школы
ALTER TABLE organizations 
  ADD COLUMN admin_user_id UUID REFERENCES users(id) ON DELETE SET NULL;

CREATE INDEX idx_organizations_admin_user_id ON organizations(admin_user_id);
```

### RLS (Row Level Security)

```sql
-- Политики для users
ALTER TABLE users ENABLE ROW LEVEL SECURITY;

-- Super Admin видит всех
CREATE POLICY "Super admins can view all users"
  ON users FOR SELECT
  USING (
    EXISTS (
      SELECT 1 FROM users u
      WHERE u.auth_user_id = auth.uid()
      AND u.role = 'super_admin'
    )
  );

-- Admin видит всех кроме super_admin
CREATE POLICY "Admins can view non-super-admin users"
  ON users FOR SELECT
  USING (
    EXISTS (
      SELECT 1 FROM users u
      WHERE u.auth_user_id = auth.uid()
      AND u.role IN ('admin', 'super_admin')
    )
    AND role != 'super_admin'
  );

-- Пользователь видит только себя
CREATE POLICY "Users can view themselves"
  ON users FOR SELECT
  USING (auth_user_id = auth.uid());
```

---

## 🔐 Авторизация и роли

### Роли пользователей

| Роль | Описание | Доступ |
|------|----------|--------|
| `super_admin` | Супер администратор | Полный доступ ко всему |
| `admin` | Администратор | Управление школами, пользователями, модерация |
| `moderator` | Модератор | Только модерация контента (отзывы, комментарии) |
| `school_admin` | Администратор школы | Управление только своей школой |
| `user` | Обычный пользователь | Просмотр контента, оставление отзывов |

### Система разрешений

```typescript
// src/lib/auth/permissions.ts

export const PERMISSIONS = {
  // Управление школами
  SCHOOLS_CREATE: ['super_admin', 'admin'],
  SCHOOLS_UPDATE: ['super_admin', 'admin', 'school_admin'],
  SCHOOLS_DELETE: ['super_admin', 'admin'],
  SCHOOLS_VIEW_ALL: ['super_admin', 'admin', 'moderator'],
  
  // Управление пользователями
  USERS_CREATE: ['super_admin', 'admin'],
  USERS_UPDATE: ['super_admin', 'admin'],
  USERS_DELETE: ['super_admin'],
  USERS_VIEW_ALL: ['super_admin', 'admin'],
  
  // Модерация
  REVIEWS_MODERATE: ['super_admin', 'admin', 'moderator'],
  CONTENT_MODERATE: ['super_admin', 'admin', 'moderator'],
  
  // Аналитика
  ANALYTICS_VIEW: ['super_admin', 'admin'],
  
  // Настройки
  SETTINGS_UPDATE: ['super_admin'],
} as const;

export function hasPermission(userRole: string, permission: keyof typeof PERMISSIONS): boolean {
  return PERMISSIONS[permission].includes(userRole as any);
}
```

---

## 📊 Функциональность админ панели

### 1. Dashboard (Главная страница)

**URL**: `/admin/dashboard`

**Компоненты:**
- Статистика платформы:
  - Всего школ
  - Активных школ
  - Всего пользователей
  - Новых школ за месяц
  - Ожидающих модерации
- Графики:
  - Рост школ по месяцам
  - Распределение по типам
  - Топ районов
- Последние действия (activity log)
- Быстрые действия (quick actions)

---

### 2. Управление школами

**URL**: `/admin/schools`

#### 2.1 Список школ (`/admin/schools`)

**Функции:**
- Таблица со всеми школами
- Фильтры:
  - Статус (active, pending, archived)
  - Тип школы
  - Район/город
  - Верификация
- Сортировка по колонкам
- Поиск по названию
- Массовые действия (активация, архивация)
- Экспорт в CSV/Excel

**Колонки таблицы:**
- Название
- Тип
- Район/Город
- Статус
- Верификация
- Рейтинг
- Дата создания
- Действия (редактировать, удалить, просмотреть)

#### 2.2 Создание школы (`/admin/schools/new`)

**Функции:**
- Многошаговая форма:
  1. Базовая информация (название, тип, slug)
  2. Контакты и адрес
  3. Детали школы (классы, языки, программа)
  4. Финансы и услуги
  5. Инфраструктура
  6. Документы
- Валидация всех полей
- Предпросмотр перед сохранением
- Загрузка изображений (логотип, обложка)

#### 2.3 Редактирование школы (`/admin/schools/[id]`)

**Функции:**
- Та же форма, но с предзаполненными данными
- История изменений (audit log)
- Возможность откатить изменения
- Управление филиалами
- Управление изображениями

---

### 3. Управление пользователями

**URL**: `/admin/users`

**Функции:**
- Список всех пользователей
- Фильтры по роли, статусу
- Создание пользователя
- Редактирование роли
- Блокировка/разблокировка
- Привязка к школе (для school_admin)

---

### 4. Модерация отзывов

**URL**: `/admin/reviews`

**Функции:**
- Список отзывов, ожидающих модерации
- Одобрение/отклонение отзывов
- Редактирование отзывов
- Блокировка пользователей за спам

---

### 5. Аналитика

**URL**: `/admin/analytics`

**Функции:**
- Статистика по школам
- Статистика по пользователям
- Популярные школы
- География (карта с распределением)
- Экспорт отчетов

---

### 6. Настройки платформы

**URL**: `/admin/settings`

**Функции:**
- Общие настройки
- Email настройки
- Интеграции (API ключи)
- Резервное копирование
- Логи системы

---

## 🚀 Этапы реализации

### Этап 1: Базовая инфраструктура (2-3 дня)

**Задачи:**
1. ✅ Создать таблицу `users` в БД
2. ✅ Настроить Supabase Auth
3. ✅ Создать систему ролей и разрешений
4. ✅ Создать middleware для проверки доступа
5. ✅ Создать страницы авторизации (login/register)
6. ✅ Создать базовый layout админ панели

**Результат:**
- Работающая авторизация
- Защищенные маршруты
- Базовый UI админ панели

---

### Этап 2: Dashboard (1-2 дня)

**Задачи:**
1. Создать страницу `/admin/dashboard`
2. Реализовать виджеты статистики
3. Добавить графики (использовать recharts или chart.js)
4. Создать activity log

**Результат:**
- Рабочий dashboard с основной статистикой

---

### Этап 3: Управление школами — Список (2-3 дня)

**Задачи:**
1. Создать компонент DataTable (с сортировкой, фильтрацией, пагинацией)
2. Создать страницу `/admin/schools`
3. Реализовать фильтры и поиск
4. Добавить массовые действия
5. Добавить экспорт данных

**Результат:**
- Полнофункциональный список школ

---

### Этап 4: Управление школами — CRUD (3-4 дня)

**Задачи:**
1. Создать форму создания школы (`/admin/schools/new`)
2. Создать форму редактирования (`/admin/schools/[id]`)
3. Реализовать валидацию (Zod)
4. Добавить загрузку изображений
5. Реализовать удаление школы
6. Добавить подтверждение перед удалением

**Результат:**
- Полный CRUD для школ

---

### Этап 5: Управление пользователями (2-3 дня)

**Задачи:**
1. Создать страницу `/admin/users`
2. Реализовать список пользователей
3. Добавить создание/редактирование пользователей
4. Реализовать управление ролями
5. Добавить блокировку пользователей

**Результат:**
- Управление пользователями

---

### Этап 6: Модерация и аналитика (2-3 дня)

**Задачи:**
1. Создать страницу модерации отзывов
2. Добавить базовую аналитику
3. Создать экспорт отчетов

**Результат:**
- Модерация контента
- Базовая аналитика

---

## 🛠 Технические детали

### Компоненты shadcn/ui для использования

```bash
# Уже установлены
- button
- card
- tabs
- select
- checkbox
- slider
- badge

# Нужно установить
npx shadcn@latest add table        # Для DataTable
npx shadcn@latest add dialog       # Для модальных окон
npx shadcn@latest add form         # Для форм
npx shadcn@latest add input        # Для input полей
npx shadcn@latest add textarea     # Для textarea
npx shadcn@latest add label        # Для labels
npx shadcn@latest add dropdown-menu # Для меню действий
npx shadcn@latest add alert        # Для уведомлений
npx shadcn@latest add toast        # Для toast уведомлений
npx shadcn@latest add avatar       # Для аватаров
npx shadcn@latest add separator    # Для разделителей
npx shadcn@latest add sheet        # Для боковых панелей
```

### Библиотеки для установки

```bash
# Формы и валидация
npm install react-hook-form zod @hookform/resolvers

# Таблицы
npm install @tanstack/react-table

# Графики (опционально)
npm install recharts

# Загрузка файлов
# Используем Supabase Storage (уже есть)
```

### Middleware для защиты маршрутов

```typescript
// src/middleware.ts
import { createMiddlewareClient } from '@supabase/auth-helpers-nextjs';
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export async function middleware(req: NextRequest) {
  const res = NextResponse.next();
  const supabase = createMiddlewareClient({ req, res });

  const {
    data: { session },
  } = await supabase.auth.getSession();

  // Защита админ маршрутов
  if (req.nextUrl.pathname.startsWith('/admin')) {
    if (!session) {
      return NextResponse.redirect(new URL('/auth/login', req.url));
    }

    // Проверка роли пользователя
    const { data: user } = await supabase
      .from('users')
      .select('role')
      .eq('auth_user_id', session.user.id)
      .single();

    if (!user || !['super_admin', 'admin', 'moderator'].includes(user.role)) {
      return NextResponse.redirect(new URL('/', req.url));
    }
  }

  return res;
}

export const config = {
  matcher: ['/admin/:path*'],
};
```

---

## 📝 Чеклист готовности

### Перед началом разработки
- [ ] Создать задачи в Linear
- [ ] Спроектировать UI/UX (можно использовать Figma или просто описание)
- [ ] Подготовить тестовые данные

### После завершения
- [ ] Протестировать все функции
- [ ] Написать документацию для админов
- [ ] Создать первого super_admin пользователя
- [ ] Настроить резервное копирование

---

## 🔗 Связанные документы

- [DATABASE_SCHEMA.md](../schools/DATABASE_SCHEMA.md) — Схема БД для школ
- [PROJECT_CONTEXT.md](../../PROJECT_CONTEXT.md) — Общий контекст проекта

---

*Последнее обновление: Декабрь 2025*

