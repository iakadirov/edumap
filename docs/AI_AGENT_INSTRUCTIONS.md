# Инструкции для ИИ агента — EduMap.uz

> **Важно:** Этот документ содержит правила работы для ИИ агентов (Claude, Cursor Agent, и т.д.)

---

## 🎯 Главное правило

**Все спринты и задачи ДОЛЖНЫ быть созданы в Linear с четкими описаниями перед началом работы.**

---

## 📋 Правила работы с задачами

### 1. Создание задач в Linear

Перед началом работы над любой функциональностью:

1. ✅ **Создать задачи в Linear** с подробными описаниями
2. ✅ **Использовать правильный формат заголовка:** `[МЕТКА] Название задачи`
3. ✅ **Включить в описание:**
   - Что нужно сделать (четко и понятно)
   - Какие файлы создать/изменить
   - Технические требования
   - Ссылки на документацию
   - Компоненты shadcn/ui (если нужны)

### 2. Формат заголовка задач

```
[МЕТКА] Название задачи
```

**Метки:**
- `[UI]` - задачи по интерфейсу и компонентам
- `[DB]` - задачи по базе данных и миграциям
- `[SCHOOLS]` - задачи по модулю школ
- `[SETUP]` - задачи по настройке проекта
- `[DOCS]` - задачи по документации
- `[API]` - задачи по API endpoints
- `[TEST]` - задачи по тестированию

**Примеры:**
- `[UI] Создать Header с навигацией`
- `[SCHOOLS] Создать компонент SchoolCard`
- `[DB] Добавить индекс для поиска школ`

### 3. Структура описания задачи

```markdown
Описание того, что нужно сделать.

Требования:
- Требование 1
- Требование 2
- Требование 3

Файлы:
- src/path/to/file.tsx
- src/path/to/file.ts

Компоненты shadcn/ui:
- npx shadcn@latest add component-name

Интеграция:
- Использовать функцию из src/lib/...

Ссылки:
- docs/path/to/doc.md
- Внешняя документация
```

### 4. Приоритеты задач

- `1` - No priority (низкий)
- `2` - Medium (средний, по умолчанию)
- `3` - High (высокий)
- `4` - Critical (критичный)

---

## 🔄 Процесс работы

### Этап 1: Планирование

1. Изучить `docs/CURRENT_SPRINT.md` или `docs/NEXT_STEPS.md`
2. Определить список задач для выполнения
3. **Создать все задачи в Linear** с подробными описаниями
4. Назначить приоритеты

### Этап 2: Выполнение

1. Работать над задачами по одной
2. Начинать с задач High priority (3)
3. Обновлять статус задачи в Linear при начале работы
4. После завершения — отмечать как Done

### Этап 3: Проверка

1. Проверить, что все требования выполнены
2. Убедиться, что код соответствует стандартам проекта
3. Обновить документацию при необходимости
4. Отметить задачу как Done в Linear

---

## 🛠 Использование Linear API

### Функции для работы с задачами

```typescript
import { 
  getAllIssues, 
  getTodoIssues, 
  getInProgressIssues,
  createIssue,
  updateIssue,
  getTeams
} from '@/lib/linear';

// Получить все задачи
const issues = await getAllIssues();

// Получить задачи готовые к работе
const todos = await getTodoIssues();

// Создать задачу
const issue = await createIssue({
  teamId: 'team-id',
  title: '[UI] Название задачи',
  description: 'Подробное описание...',
  priority: 3, // High
});

// Обновить статус задачи
await updateIssue(issueId, {
  stateId: 'completed-state-id',
});
```

### Получение ID команды

```typescript
const teams = await getTeams();
const team = teams.find(t => t.key === 'EDU'); // Edumap команда
// team.id - использовать для создания задач
```

---

## 📚 Источники контекста

### Перед началом работы

1. Прочитать `docs/PROJECT_CONTEXT.md` — главный контекст проекта
2. Проверить `docs/CURRENT_SPRINT.md` — текущие цели спринта
3. Изучить `docs/NEXT_STEPS.md` — следующие шаги
4. Посмотреть `docs/modules/` — документацию по модулям

### При работе над конкретной задачей

1. Изучить описание задачи в Linear (всегда читать полностью!)
2. Проверить связанные файлы и компоненты
3. Изучить документацию модуля (если есть)
4. Проверить существующие паттерны в коде

---

## ✅ Checklist перед созданием задачи

- [ ] Заголовок в формате `[МЕТКА] Название`
- [ ] Подробное описание с требованиями
- [ ] Указаны файлы для создания/изменения
- [ ] Указаны компоненты shadcn/ui (если нужны)
- [ ] Добавлены ссылки на документацию
- [ ] Установлен правильный приоритет
- [ ] Задача создана в правильной команде (EDU)

---

## 🎯 Пример хорошей задачи

```
Заголовок: [UI] Создать Header с навигацией

Описание:
Создать Header компонент с навигацией и логотипом.

Требования:
- Логотип EduMap.uz (можно временно текст)
- Основная навигация: Главная, Школы, О проекте
- Мобильное меню (hamburger menu)
- Responsive дизайн
- Использовать shadcn/ui компоненты (Navigation Menu)

Файлы:
- src/components/shared/Header.tsx
- Добавить в Layout компонент

Компоненты shadcn/ui:
- npx shadcn@latest add navigation-menu

Ссылки:
- docs/CURRENT_SPRINT.md → UI Foundation
```

---

## 🚫 Что НЕ делать

- ❌ Начинать работу без создания задачи в Linear
- ❌ Создавать задачи с недостаточным описанием
- ❌ Пропускать этап планирования
- ❌ Работать над несколькими задачами одновременно без координации
- ❌ Забывать обновлять статусы задач в Linear

---

## 📖 Дополнительные ресурсы

- [Документация Linear API](./LINEAR_API_SETUP.md)
- [Интеграция Cursor ↔ Linear](./CURSOR_LINEAR_INTEGRATION.md)
- [Процесс работы](./WORKFLOW.md)

---

*Последнее обновление: Декабрь 2025*

