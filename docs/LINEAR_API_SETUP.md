# Настройка Linear API для работы с задачами

## ✅ Что уже настроено

В проекте уже настроена полная интеграция с Linear API:

- ✅ Установлен `@linear/sdk`
- ✅ Создан клиент Linear (`src/lib/linear/client.ts`)
- ✅ Реализованы функции для работы с задачами (`src/lib/linear/issues.ts`)
- ✅ Настроены типы TypeScript (`src/lib/linear/types.ts`)
- ✅ Созданы скрипты для работы из командной строки
- ✅ Добавлены API endpoints

## 🔑 Что нужно сделать ВАМ

### Шаг 1: Получить API ключ из Linear

1. Откройте https://linear.app и войдите в свой аккаунт
2. Перейдите в **Settings** → **API** → **Personal API keys**
   - Или прямо по ссылке: https://linear.app/settings/api
3. Нажмите **"Create new key"** или **"New API Key"**
4. Дайте ключу название (например: "EduMap Development")
5. Скопируйте созданный API ключ (он начинается с `lin_api_...`)

### Шаг 2: Добавить ключ в проект

Создайте файл `.env.local` в корне проекта (если его еще нет):

```bash
# .env.local
LINEAR_API_KEY=lin_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

⚠️ **Важно:**
- Замените `lin_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxx` на ваш реальный ключ
- Файл `.env.local` уже в `.gitignore`, так что ключ не попадет в Git
- После добавления ключа перезапустите dev-сервер (`npm run dev`)

### Шаг 3: Проверить подключение

Выполните команду:

```bash
npm run linear:list
```

Если всё настроено правильно, вы увидите список всех задач из Linear.

## 🎯 Что можно делать после настройки

### Я могу создавать задачи в Linear

Просто скажите мне, какую задачу создать, например:
- "Создай задачу 'Добавить фильтр по району'"
- "Создай задачу с описанием: 'Реализовать поиск школ'"

Или используйте скрипт:
```bash
npm run linear:create
```

### Я могу читать задачи из Linear

Спросите меня:
- "Какие задачи есть в Linear?"
- "Покажи задачи на сегодня"
- "Какие задачи в работе?"

Или используйте команды:
```bash
npm run linear:list      # Все задачи
npm run linear:todo      # Только задачи со статусом "Todo"
```

### Я могу изменять статус задач

Скажите мне:
- "Отметь задачу EDU-123 как выполненную"
- "Переведи задачу EDU-456 в статус 'In Progress'"

### Я могу обновлять задачи

- "Обнови описание задачи EDU-123"
- "Измени приоритет задачи EDU-456 на High"

## 📋 Доступные функции

### В коде проекта (`src/lib/linear/issues.ts`)

```typescript
// Получить все задачи
getAllIssues(teamId?: string): Promise<LinearIssue[]>

// Получить задачи в работе
getInProgressIssues(teamId?: string): Promise<LinearIssue[]>

// Получить задачи готовые к работе (Todo)
getTodoIssues(teamId?: string): Promise<LinearIssue[]>

// Создать новую задачу
createIssue(input: LinearIssueInput): Promise<LinearIssue>

// Обновить задачу
updateIssue(
  issueId: string,
  updates: {
    title?: string
    description?: string
    stateId?: string      // Изменить статус
    assigneeId?: string
    priority?: number
  }
): Promise<LinearIssue>

// Получить список команд
getTeams(): Promise<LinearTeam[]>
```

### API Endpoints

После настройки ключа доступны:

- `GET /api/linear/issues` - получить все задачи
- `GET /api/linear/teams` - получить список команд

### Командная строка

```bash
npm run linear:list           # Список всех задач
npm run linear:todo           # Задачи со статусом Todo
npm run linear:create         # Создать задачу интерактивно
npm run linear:create-today   # Создать задачи на сегодня
```

## 🔒 Безопасность

- ✅ API ключ хранится в `.env.local` (не коммитится в Git)
- ✅ Для production (Vercel) нужно добавить ключ в Environment Variables
- ⚠️ **Не делитесь ключом публично** - он дает полный доступ к вашему Linear workspace

## 🐛 Troubleshooting

### Ошибка: "LINEAR_API_KEY не найден в переменных окружения"

**Решение:**
1. Проверьте, что файл `.env.local` существует в корне проекта
2. Проверьте, что ключ записан правильно (начинается с `lin_api_...`)
3. Перезапустите dev-сервер

### Ошибка: "Unauthorized" или "Invalid API key"

**Решение:**
1. Проверьте, что ключ скопирован полностью (без пробелов)
2. Создайте новый ключ в Linear, возможно старый был удален
3. Убедитесь, что ключ активен в Linear Settings → API

### Задачи не находятся

**Решение:**
1. Проверьте, что в Linear есть задачи
2. Проверьте, что вы используете правильный `teamId` (если указан)
3. Попробуйте `getAllIssues()` без `teamId`

## 🚀 После настройки

После добавления API ключа я смогу:

1. ✅ **Создавать задачи** прямо из чата
2. ✅ **Читать список задач** и помогать планировать работу
3. ✅ **Обновлять статусы** задач после выполнения
4. ✅ **Изменять приоритеты** и назначать исполнителей
5. ✅ **Синхронизировать работу** между Linear и кодом

**Начните с шага 1 — получите API ключ из Linear!**

