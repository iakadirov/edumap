# Настройка кастомного домена для EduMap.uz

> **Важно:** На бесплатном тарифе Vercel можно подключить неограниченное количество кастомных доменов!

---

## 🎯 Быстрая инструкция

1. **Vercel Dashboard** → Ваш проект → **Settings** → **Domains**
2. Нажмите **Add Domain**
3. Введите `edumap.uz` (или `www.edumap.uz`)
4. Добавьте DNS записи в вашем регистраторе домена
5. Подождите 5-30 минут
6. ✅ Готово! SSL сертификат выдается автоматически

---

## 📋 Подробная инструкция

### Шаг 1: Добавить домен в Vercel

1. Откройте https://vercel.com/dashboard
2. Выберите проект **edumap**
3. Перейдите в **Settings** → **Domains**
4. Нажмите кнопку **Add Domain**
5. Введите ваш домен:
   - Для корневого: `edumap.uz`
   - Для www: `www.edumap.uz` (опционально)
6. Нажмите **Add**

### Шаг 2: Настроить DNS записи

Vercel покажет вам инструкции. Есть два способа:

#### Способ 1: Использовать Vercel DNS (самый простой)

Если ваш регистратор домена позволяет изменить NS записи:

1. Vercel даст вам NS записи (например: `ns1.vercel-dns.com`)
2. В панели вашего регистратора домена:
   - Найдите раздел "Nameservers" или "NS записи"
   - Замените текущие NS записи на те, что дал Vercel
3. Сохраните изменения
4. Vercel автоматически настроит все DNS записи

**Регистраторы, которые поддерживают смену NS:**
- GoDaddy
- Namecheap
- Cloudflare
- Google Domains
- И большинство других

#### Способ 2: Настроить DNS вручную

Если вы не можете изменить NS записи, добавьте DNS записи вручную:

**В панели вашего регистратора домена:**

1. Найдите раздел "DNS Records" или "DNS Management"

2. **Для корневого домена (edumap.uz):**

   **Вариант A: A запись (если поддерживается)**
   ```
   Type: A
   Name: @ (или оставьте пустым)
   Value: 76.76.21.21
   TTL: 3600 (или Auto)
   ```

   **Вариант B: CNAME запись (если A не работает)**
   ```
   Type: CNAME
   Name: @ (или оставьте пустым)
   Value: cname.vercel-dns.com
   TTL: 3600 (или Auto)
   ```

   **Вариант C: ANAME/ALIAS (если поддерживается)**
   ```
   Type: ANAME (или ALIAS)
   Name: @
   Value: cname.vercel-dns.com
   TTL: 3600
   ```

3. **Для поддомена www (опционально):**
   ```
   Type: CNAME
   Name: www
   Value: cname.vercel-dns.com
   TTL: 3600
   ```

4. Сохраните изменения

### Шаг 3: Проверить DNS

1. Вернитесь в Vercel Dashboard
2. В разделе **Domains** нажмите **Refresh**
3. Vercel автоматически проверит DNS записи
4. Статус должен измениться на ✅ **Valid** (может занять 5-30 минут)

### Шаг 4: Дождаться SSL сертификата

- Vercel автоматически выдает SSL сертификат (Let's Encrypt)
- Обычно это занимает **5-30 минут**
- После выдачи сертификата ваш сайт будет доступен по HTTPS

---

## 🔍 Проверка работы

### Проверить DNS записи

Используйте онлайн инструменты:
- https://dnschecker.org
- https://www.whatsmydns.net

Введите ваш домен и проверьте, что записи правильные.

### Проверить в терминале

```bash
# Проверить A запись
nslookup edumap.uz

# Проверить CNAME
nslookup www.edumap.uz

# Проверить все записи
dig edumap.uz ANY
```

### Проверить в браузере

1. Откройте `https://edumap.uz` (или `http://edumap.uz` - должно перенаправить на HTTPS)
2. Проверьте, что в адресной строке есть замочек 🔒
3. Убедитесь, что сайт загружается правильно

---

## ⚙️ Настройка для популярных регистраторов

### GoDaddy

1. Войдите в аккаунт GoDaddy
2. Перейдите в **My Products** → **DNS**
3. Найдите ваш домен и нажмите **Manage DNS**
4. Добавьте записи:
   - **Type:** A, **Name:** @, **Value:** 76.76.21.21
   - **Type:** CNAME, **Name:** www, **Value:** cname.vercel-dns.com
5. Сохраните изменения

### Namecheap

1. Войдите в аккаунт Namecheap
2. Перейдите в **Domain List** → выберите домен → **Manage**
3. Перейдите на вкладку **Advanced DNS**
4. Добавьте записи:
   - **Type:** A Record, **Host:** @, **Value:** 76.76.21.21
   - **Type:** CNAME Record, **Host:** www, **Value:** cname.vercel-dns.com
5. Сохраните изменения

### Cloudflare

1. Войдите в Cloudflare Dashboard
2. Выберите ваш домен
3. Перейдите в **DNS** → **Records**
4. Добавьте записи:
   - **Type:** A, **Name:** @, **IPv4 address:** 76.76.21.21, **Proxy:** Off (серый облачко)
   - **Type:** CNAME, **Name:** www, **Target:** cname.vercel-dns.com, **Proxy:** Off
5. Сохраните изменения

**Важно:** В Cloudflare отключите Proxy (серый облачко) для правильной работы с Vercel.

---

## ❓ FAQ

### Можно ли использовать бесплатный домен?

Да! Vercel не различает платные и бесплатные домены. Любой домен, который вы можете настроить через DNS, будет работать.

### Сколько доменов можно добавить?

На Free плане: **неограниченное количество** доменов!

### Нужно ли платить за SSL?

Нет! Vercel автоматически выдает бесплатные SSL сертификаты (Let's Encrypt) для всех доменов.

### Можно ли использовать поддомены?

Да! Вы можете добавить любой поддомен:
- `www.edumap.uz`
- `admin.edumap.uz`
- `api.edumap.uz`
- И т.д.

### Что делать, если DNS не работает?

1. Проверьте, что записи добавлены правильно
2. Подождите до 24 часов (DNS может распространяться медленно)
3. Очистите DNS кэш: `ipconfig /flushdns` (Windows) или `sudo dscacheutil -flushcache` (Mac)
4. Используйте другой DNS сервер (например, Google DNS: 8.8.8.8)

### Можно ли использовать домен с других платформ?

Да! Вы можете использовать домен, купленный у любого регистратора. Главное - правильно настроить DNS записи.

---

## 🎉 Готово!

После настройки ваш сайт будет доступен по адресу:
- **HTTPS:** https://edumap.uz
- **WWW:** https://www.edumap.uz (если настроили)

Vercel автоматически перенаправляет HTTP на HTTPS, так что пользователи всегда будут использовать безопасное соединение.

---

*Последнее обновление: Декабрь 2025*

